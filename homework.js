//HOMEWORK
//1- js function that can give the average between two number
             var num1 = 10 ;
             var num2 = 2;
             console.log(10,2)
             
             function average(num1, num2){
             return   (num1 + num2)  / 2 ;
             }
            
//2- js function which permit us to test if a number is odd or even by returning 1 if its even and 0 if its odd
             function  isEven(num){
             if ( num / 2 == 0 ) {
             return 1;  //for even
             }
             else {
             return 0 ; //for odd
             }
                 }

